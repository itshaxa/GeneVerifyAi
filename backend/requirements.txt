# GeneVerify AI — runtime dependencies (Step 11: deployment manifest)
#
# These are the only packages needed to RUN the API. Test tooling lives in
# requirements-dev.txt so the production image stays lean.

fastapi>=0.116,<1.0
uvicorn>=0.35,<1.0
sqlalchemy>=2.0,<3.0
pydantic>=2.11,<3.0
pydantic-settings>=2.9,<3.0
python-dotenv>=1.1,<2.0
argon2-cffi>=25.1,<26.0
PyJWT>=2.10,<3.0
python-multipart>=0.0.20,<1.0

# Step 9: server-side PDF report generation
fpdf2>=2.8,<3.0
